#include "Common.h"

// 实际定义全局变量（分配内存）
double GAME_SCALE = 1; // 定义
int GAME_WIDTH = 640;  // 定义
int GAME_HEIGHT = 160; // 定义
int GAME_OFFSET_X = 0; // 定义
int GAME_OFFSET_Y = 0; // 定义
int GAME_LINE = 126;   // 定义